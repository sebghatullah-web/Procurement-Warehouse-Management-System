# راهنمای روند درخواست مواد و وسایل مورد ضرورت
## از ثبت درخواست تا تحویل نهایی به اداره

**سامانه مدیریت تدارکات و انبار (PWMS) — شرکت انکشافی خاور**
سند ارائه برای جلسه تدارکات و گدام‌داری.

> نسخه PDF این سند: `docs/PWMS_Process_Guide_FA.pdf`
> نسخه قابل چاپ (منبع): `docs/workflow_meeting_fa.html`
> برای بازتولید PDF: فایل HTML را در مرورگر باز کرده و «Print → Save as PDF» بزنید (اندازه A4).

---

## ۱. نمای کلی روند

هر درخواست مواد یا وسایل یک مسیر استاندارد و قابل ردیابی را طی می‌کند. در هر مرحله وضعیت درخواست تغییر می‌کند و فرد مسئول و زمان انجام ثبت می‌شود.

```
ثبت درخواست ← بررسی تدارکات ← بررسی انبار ← شروع خرید ← جمع‌آوری قیمت
   ← تصمیم کمیته ← سفارش نهایی ← رسید گیت ← ذخیره یا تحویل (پایان)
```

- **مسیر فرعی ۱:** اگر کالا در انبار موجود باشد، روند در «گام ۳» با صدور از انبار پایان می‌یابد (بدون خرید).
- **مسیر فرعی ۲:** اگر درخواست غبرضروری تشخیص داده شود، در گام ۲ بسته می‌شود و خاتمه می‌یابد.
- **مسیر فرعی ۳:** در «خرید فوری»، گام‌های جمع‌آوری قیمت و کمیته رد می‌شوند.

---

## ۲. گام‌های روند به تفصیل

### گام ۱ — ثبت درخواست توسط کارمند
`requests/create.php` — وضعیت: **در انتظار بررسی**

فورم شامل: اداره درخواست‌کننده، نام کارمند و موقعیت وظیفه‌ای (پست)، نام کالا، تعداد و واحد، دسته‌بندی کالا، فوریت (عادی / فوری)، تحویل مستقیم، برآورد قیمت با واحد پول (افغانی AFN / دالر USD)، تاریخ مورد نیاز، دلیل و جزئیات تکمیلی.

نتیجه: صدور خودکار شماره درخواست (مثلاً `REQ-0001`) و قرارگیری در صف مدیر تدارکات.

### گام ۲ — بررسی و تصمیم مدیر تدارکات
`requests/review.php`

- **تأیید (ضروری):** ارسال برای بررسی انبار ← وضعیت **بررسی انبار** (ثبت تأییدکننده و زمان).
- **بستن (غیرضروری):** با ذکر دلیل الزامی ← وضعیت **بسته شده** و **پایان روند**.

### گام ۳ — بررسی موجودی انبار و صدور
`warehouse/check.php` — مدیر انبار

- **موجود و کافی:** «صدور به اداره» ← کسر از موجودی + ثبت مصرف ← وضعیت **کامل شده** و **پایان روند**.
- **موجود نیست / ناکافی:** «ارسال به فرآیند خرید» ← وضعیت **نیاز به خرید**.

### گام ۴ — شروع فرآیند خرید
`purchases/create.php` — مدیر تدارکات

- **خرید عادی:** ایجاد خرید با وضعیت **جمع‌آوری قیمت‌ها**.
- **خرید فوری:** انتخاب تأمین‌کننده و قیمت (AFN/USD) و ثبت مستقیم سفارش.

### گام ۵ — جمع‌آوری قیمت‌ها
`purchases/view.php` — مدیر تدارکات

ثبت حداقل **۳ قیمت** از تأمین‌کنندگان (تأمین‌کننده، قیمت، واحد پول، زمان تحویل، یادداشت) و سپس «ارسال به کمیته» ← وضعیت **در انتظار کمیته**.

### گام ۶ — تصمیم کمیته
`purchases/view.php` — اعضای کمیته (مالی، مدیریت، تدارکات)

- **تصویب** ← وضعیت **تصویب شده**.
- **رد** ← وضعیت **رد شده**؛ امکان اصلاح قیمت‌گیری و ارسال مجدد.

### گام ۷ — ثبت سفارش نهایی
`purchases/view.php` — مدیر تدارکات: انتخاب تأمین‌کننده برنده و ثبت سفارش ← وضعیت **سفارش داده شده**.

### گام ۸ — رسید و بازرسی گیت
`gate/receipts.php` — امنیت گیت: ثبت تعداد دریافت‌شده، سلامت کالا و یادداشت ← وضعیت **دریافت شده در گیت**.

### گام ۹ — تکلیف نهایی (تحویل)
`warehouse/disposition.php` — مدیر انبار

- **ذخیره در انبار:** افزودن کالا به موجودی (نام، دسته، تعداد، واحد، موقعیت، حداقل موجودی).
- **تحویل مستقیم به اداره:** ثبت تحویل و رکورد مصرف.

در هر دو حالت وضعیت **کامل شده** ← **پایان روند** و کالا به دست اداره درخواست‌کننده می‌رسد.

---

## ۳. وضعیت‌های درخواست

| وضعیت | معنا | مرحله روند |
|---|---|---|
| در انتظار بررسی | ثبت شده و در صف مدیر تدارکات است | گام ۱ تا ۲ |
| بسته شده (غیرضروری) | غیرضروری اعلام شد؛ روند خاتمه یافت | گام ۲ |
| بررسی انبار | تأیید شده و موجودی در حال بررسی است | گام ۳ |
| نیاز به خرید | در انبار موجود نیست؛ خرید لازم است | گام ۳ تا ۴ |
| در حال جمع‌آوری قیمت‌ها | قیمت‌ها از تأمین‌کنندگان جمع می‌شود | گام ۵ |
| در انتظار کمیته | قیمت‌ها آماده و در انتظار تصمیم کمیته | گام ۶ |
| تصویب شده | کمیته تأیید کرد؛ آماده ثبت سفارش | گام ۶ تا ۷ |
| خریداری / سفارش داده شده | سفارش نهایی با تأمین‌کننده ثبت شد | گام ۷ |
| دریافت شده در گیت | کالا رسید و چک‌لیست گیت تکمیل شد | گام ۸ |
| کامل شده | ذخیره در انبار یا تحویل به اداره شد؛ **پایان روند** | گام ۳الف یا ۹ |

## ۴. وضعیت‌های خرید

| وضعیت خرید | معنا |
|---|---|
| پیش‌نویس / جمع‌آوری قیمت‌ها | سفارش باز شده و قیمت‌ها در حال جمع‌آوری است |
| در انتظار کمیته | قیمت‌ها آماده و برای کمیته ارسال شده است |
| تصویب شده | تصمیم کمیته مثبت است |
| رد شده | کمیته رد کرده؛ امکان اصلاح و ارسال مجدد |
| سفارش داده شده | سفارش نهایی ثبت شد (شامل خرید فوری) |
| دریافت شده در گیت | چک‌لیست امنیت تکمیل شد |
| کامل شده | تعیین تکلیف انجام شد (ذخیره یا تحویل مستقیم) |

## ۵. نقش‌ها و مسئولیت‌ها

| نقش | مسئولیت‌های اصلی |
|---|---|
| کارمند | ثبت درخواست، پیگیری وضعیت درخواست‌های خود |
| مدیر تدارکات | بررسی و تأیید/بستن درخواست، شروع خرید، جمع‌آوری قیمت‌ها، ارسال به کمیته، ثبت سفارش، مدیریت تأمین‌کنندگان |
| مدیر انبار (گدام) | بررسی موجودی، صدور از انبار، مدیریت اقلام و دسته‌بندی‌ها، تعیین تکلیف خرید |
| امنیت گیت | اجرای چک‌لیست رسید کالاهای ورودی |
| عضو کمیته | تصمیم تأیید یا رد خریدهای در انتظار کمیته |
| مدیر عمومی | مشاهده داشبورد استراتژیک و گزارشات |
| مدیر سیستم | مدیریت کاربران، ادارات و نظارت کامل بر سامانه |

## ۶. کنترل، شفافیت و پیگیری

- **تاریخچه کامل:** یادداشت بررسی، یادداشت انبار، آرا کمیته، چک‌لیست گیت و یادداشت تحویل ثبت می‌شود.
- **مسئولیت‌پذیری:** نام فرد انجام‌دهنده و زمان هر اقدام ذخیره می‌شود.
- **پشتیبانی ضد کمبود:** صدور از انبار بدون موجودی کافی ممکن نیست.
- **ارز شفاف:** قیمت‌ها با واحد پول مشخص (AFN یا USD) ثبت می‌شوند.
- **گزارش‌ها:** خرید، مصرف، موجودی و تحلیل هزینه‌ها در دسترس مدیران است.
- **بایگانی:** درخواست‌های بسته و کامل شده محفوظ می‌مانند.

## ۷. ضمیمه — صفحه‌های کلیدی سیستم

| نقش | صفحه/کارکرد | فایل |
|---|---|---|
| کارمند | ثبت درخواست جدید | `requests/create.php` |
| کارمند / همه | لیست و پیگیری درخواست‌ها | `requests/list.php` |
| مدیر تدارکات | بررسی و تأیید/بستن درخواست | `requests/review.php` |
| مدیر انبار | بررسی موجودی و صدور | `warehouse/check.php` |
| مدیر تدارکات | ایجاد خرید (عادی/فوری) | `purchases/create.php` |
| تدارکات / کمیته | جمع‌آوری قیمت، تصمیم کمیته، سفارش | `purchases/view.php` |
| امنیت گیت | چک‌لیست رسید کالا | `gate/receipts.php` |
| مدیر انبار | ذخیره در انبار یا تحویل مستقیم | `warehouse/disposition.php` |
| مدیر انبار | مدیریت موجودی و اقلام | `warehouse/inventory.php` |
| مدیران | گزارش خرید، مصرف، تحلیل هزینه‌ها | `reports/` |

---

**نتیجه‌گیری:** سامانه مسیر درخواست مواد/وسایل را از ثبت تا تحویل نهایی به شکل استاندارد، مستند و قابل پیگیری اجرا می‌کند؛ هر مرحله مسئول مشخص، وضعیت قابل مشاهده و ثبت الکترونیکی دارد و در نهایت کالا با «ذخیره در انبار» یا «تحویل مستقیم به اداره» به درخواست‌کننده می‌رسد.

*این سند بر اساس کد و رفتار واقعی سامانه (PWMS) تهیه شده است. — شرکت انکشافی خاور*

